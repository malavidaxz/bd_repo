version: '2.0'

services:

    db:
        image: mysql
        restart: always
        environment:
            MYSQL_ROOT_PASSWORD: 1234

    adminer:
        image: adminer
        restart: always
        ports:
            - 8080:8080